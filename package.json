{
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "format": "yarn prettier --write .",
    "format-check": "prettier --check .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "prettier": "^2.8.4",
    "pretty-quick": "^3.1.3"
  }
}
